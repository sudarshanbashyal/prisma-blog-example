# Migration `20201128110344-init`

This migration has been generated by Sudarshan Bashyal at 11/28/2020, 4:48:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Users" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "usersId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE TABLE "Blogs" (
"id" SERIAL,
    "title" TEXT NOT NULL,
    "content" TEXT,
    "authorId" INTEGER NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY ("id")
)

CREATE TABLE "Comments" (
"id" SERIAL,
    "title" TEXT NOT NULL,
    "blogId" INTEGER NOT NULL,
    "commentorId" INTEGER NOT NULL,
    "commentsId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Users.email_unique" ON "Users"("email")

ALTER TABLE "Users" ADD FOREIGN KEY("usersId")REFERENCES "Users"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Blogs" ADD FOREIGN KEY("authorId")REFERENCES "Users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comments" ADD FOREIGN KEY("blogId")REFERENCES "Blogs"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comments" ADD FOREIGN KEY("commentorId")REFERENCES "Users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Comments" ADD FOREIGN KEY("commentsId")REFERENCES "Comments"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201128110344-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Users{
+    id Int @default(autoincrement()) @id
+    name String 
+    email String @unique
+    blogs Blogs[]
+    comments Comments[]
+    followers Users[]
+}
+
+model Blogs{
+    id Int @default(autoincrement()) @id
+    title String
+    content String?
+    author Users @relation(fields: [authorId], references:[id])
+    authorId Int 
+    createdAt DateTime @default(now())
+}
+
+model Comments{
+    id Int @default(autoincrement()) @id
+    title String
+    blog Blogs @relation(fields: [blogId], references:[id])
+    blogId Int
+    commentor Users @relation(fields: [commentorId], references:[id])
+    commentorId Int
+    subcomments Comments[]
+}
```


